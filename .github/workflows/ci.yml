name: Build Android and iOS
on:
  push:
    branches:
      - master
      - amplify
jobs:
  install-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install npm dependencies & Install Pods
        run: |
          npm install
      - name: Run iOS build
        run: |
          react-native run-ios
